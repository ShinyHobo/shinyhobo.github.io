import React from "react";
import ReactDOM from "react-dom/client";
import TimelineUI from "./TimelineUI";

const root = ReactDOM.createRoot(document.getElementById("root") as HTMLElement);
root.render(<TimelineUI />);